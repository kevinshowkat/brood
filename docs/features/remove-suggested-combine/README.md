# Remove Suggested Combine Overlay

This change removes the automatic `Suggested: Combine` overlay and toast.

## User view

- Importing two images no longer adds a suggested overlay between them.
- `Combine` still exists as a normal tool.

## Main file

- `desktop/src/canvas_app.js`
