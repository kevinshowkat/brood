# Multi View Pan and Zoom

This change adds wheel zoom to `Multi view`.

## User view

- You can zoom in and out while in `Multi view`.
- Zoom stays centered on the cursor.
- The HUD zoom readout reflects the current view mode.

## Main file

- `desktop/src/canvas_app.js`
